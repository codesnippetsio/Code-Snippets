const fs = require('fs');
const path = require('path');
const db = require('../server/server');

describe('unit tests on user set up', () => {

  describe('login', () => {
    // stuff in here
  });

});